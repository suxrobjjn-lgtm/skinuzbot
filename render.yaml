services:
  - type: web
    name: skenuz-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python skenuz_server.py
    plan: free
    autoDeploy: true
    envVars:
      - key: BOT_TOKEN
        sync: false

